[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
# $ pipenv install
# this should match `setup.py:setup.install_requires`
#
# To update:
# 1. force upgrade within pip virtual environment
#      python -m pip install --upgrade attrs discogs-client musicbrainzngs mutagen Pillow tabulate
# 2. run pytests (they must pass)
# 3. manually note versions installed
#      python -m pip list -v
# 4. tweak below versions with `pip list` versions
# 5. update Pipfile.lock
#      python -m pipenv update
# 6. commit changes
#      git add Pipfile Pipfile.lock setup.py
#
# XXX: these versions should match those in file setup.py
#
attrs = "==22.1.0"
#discogs-client = "2.3.0"
requests = "==2.28.1"
musicbrainzngs = "==0.7.1"
mutagen = "==1.46.0"
Pillow = "==9.3.0"
tabulate = "==0.9.0"

[dev-packages]
# $ pipenv install --dev
# this should match `setup.py:setup.extras_require`
attrs = "*"
codecov = "*"
coveralls = "*"
mypy = "*"
pip-run = "*"
pipenv = "*"
pytest = "*"
pytest-cov = "*"
pytest-dependency = "*"
pyyaml = ">=4.2b1"
setuptools = "*"
twine = "*"
wheel = "*"
yamllint = "*"

[win-packages]
# $ pipenv install --win
py2exe = "*"

[pipenv]
allow_prereleases = false
